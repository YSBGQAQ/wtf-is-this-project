# 项目速览

一个 skill 的领域模型：把别人的项目（代码仓库、HTML 文章、本地文本文件）转化成一份让纯小白读者快速读懂的产出物。

## Language

**项目速览**：
本 skill 的产出物。用简短篇幅让纯小白读者看懂一个源项目。正文按六段骨架展开。
_Avoid_: 项目介绍、项目分析、项目解剖、项目通读、README

**源项目**：
被分析的那个对象。可以是代码仓库、HTML 文章，或本地文本文件。
_Avoid_: 目标项目、输入、素材

**读者**：
使用本 skill 做简易项目理解的人。会用电脑但没写过代码——"仓库""命令行""依赖"需要解释，"变量""文件"不需要。
_Avoid_: 用户、新人、决策者、开发者

**六段骨架**：
项目速览的正文结构：怎么装上、怎么用（含装前准备）→ 它解决什么问题 → 你什么时候会用到它 → 核心概念 → 装好之后你能做到什么（写**生效信号**，不是功能清单）→ 它的边界在哪。只用两级标题。安装使用排在首位，因为那是纯小白唯一要照着动手的部分。
_Avoid_: 七段骨架、模板、结构规范、格式、大纲

**问答区**：
六段正文之后的一个合并区块，由两部分组成：2–3 组简写问答，加一份未展开细节清单（一条一句话，只给入口不展开）。它不是第七段。
_Avoid_: Common questions、追问引导、补充材料

**图表**：
项目速览中的可视化元素。**默认用纯文字 + emoji 锚点 + 箭头**——任何聊天窗口都渲染得一致，不依赖 Unicode 框图字符在特定字体的支持。**严格避免**：Unicode 框图字符与彩色 emoji 节点混用（这种组合在不同字体里字宽不一致，会乱）。有"流动"或"对比"关系的内容用图；纯文字能说清的不画。表达对象视源项目类型而定。
_Avoid_: 框图+彩色 emoji 混用、SVG 文件、复杂面板

**代码展示**：
正文中的代码片段。本 skill 中尽可能去除——目标读者看不懂，只保留追问钩子。
_Avoid_: 代码示例、代码块

**追问钩子**：
被刻意压扁到极简、仅保留入口的信息单元，典型形态是"一句话 + 函数名"。目的是让有需要的使用者能据此追问，而不是在正文里展开。
_Avoid_: 提示、引导、追问入口

**结构性表情**：
emoji 在文中承担排版职能——拿掉它读者读得更慢。按**角色**组织：方向（`➡️` `⬇️` `⬅️`）、节点（`🟦` `🟪` `🟩` `🟥`）、状态（`✅` `❌` `⚠️`）、问句（`❓`）。**同一份文档内同一 emoji 必须承担同一角色**；可换用同角色内其他 emoji。严格删除装饰性 emoji（`❤️` `🚀` `🔥` `✨` `💡` `⭐` `🎉` `👍`）。
_Avoid_: 装饰性 emoji、纯点缀

**装饰性表情**：
纯点缀、不承担排版职能的 emoji，一律删除：包括 `❤️` `🚀` `🔥` `✨` `💡` `⭐` `🎉` `👍` 等。
_Avoid_: 点缀 emoji

**说清楚为止**：
篇幅的唯一约束。不设字数上下限，由内容需要决定长度，而非由档位表决定。
_Avoid_: 分档、字数上限、自适应

**快原则**：
本 skill 的第一原则，优先级高于覆盖面。分析阶段追求最短路径，输出阶段只保证主干说透。骨架允许缺段，不允许凑数。
_Avoid_: 效率、性能优化

**不稳定数字**：
会随时间漂移、需要额外一轮核查才能确认的数字——star 数、issue 数、版本号。一律不问、不查、不写。核查本身要花时间，而漂移的数字对读者没有价值。
_Avoid_: 关键数字、项目数据、指标

**摄入分治**：
按源项目规模启用足量子 agent 并行处理原始信息，而非单线程顺序读取。上限 8 个。
_Avoid_: 并发读取、并行分析

**精读 / 粗读**：
处理 25 篇文章时的两种投入级别。精读用于提炼规律，粗读用于验证规律是否为共性、排查反例。实际执行中，粗读由跨组比对承担。
_Avoid_: 详读、略读、抽样

**归纳式分析**：
分析素材的方法论约束。不带预设框架去验证，而是从素材本身归纳规律——包括分析角度和正文结构，都不预先断言。
_Avoid_: 假设驱动、预设框架

**前言区块**：
Matt Pocock 25 篇文章的 H1 标题与第一个 H2 之间存在一个不带标题的固定区块，由安装命令、使用说明、源仓库地址三部分组成。最初被子 agent 误判为站点框架，后修正为正文结构。
_Avoid_: 元信息块、页头

**源项目标注**：
项目速览在正文最前保留的一行，明示源项目地址、语言与许可。不写版本号——那是需要复核且会漂移的数字。
_Avoid_: 头注、metadata

**平台提示词**：
humanizer 中针对 Kimi / 豆包 / DeepSeek 的改写提示词集合，用于变换行文质感。保留，但只取句法层；涉及内容顺序与篇幅的指令已删除。
_Avoid_: 降重提示词、降 AI 率指令

**中文版**：
本 skill 的首个语言版本，产出语言为中文。humanizer 的英文规则需汉化后使用。英文版是后续独立工程，不在本项目范围内。
_Avoid_: 汉化版、本地化

**文风规则**：
从 Matt Pocock 团队 25 篇文章中归纳出的写作约束，决定"精简干练"和"小白易懂"具体指什么。分 S（结构）/ W（写法）/ T（术语）/ N（不写什么）四组。
_Avoid_: 风格、tone、语气

**句法清单**：
从 humanizer 汉化而来的中文 AI 味检查清单，只管句子层面的措辞，不管内容与结构。
_Avoid_: 去 AI 味规则、润色规则
