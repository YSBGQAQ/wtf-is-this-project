# 子 agent 并发摄入设上限 8 个

源项目的原始信息按规模分档启用子 agent 并行处理：小于 20 个源文件不用子 agent，20–100 个启 3–4 个，超过 100 个或含多 package 启 6–8 个。我们把上限设为 8。

理由是超过 8 个之后，汇总各子 agent 产出的开销会吃掉并发带来的速度收益，与"快为第一原则"相悖；同时也能避免触发 API 的并发限制。选择分档而非固定数量，是因为小项目杀鸡用牛刀、大项目又不够用。

这个数字是经验值而非测算值，容易调整——如果实测发现瓶颈不在汇总而在读取，可以放宽。
